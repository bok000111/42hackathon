import styled from "@emotion/styled";

const Rank = () => {
  return <RankContainer></RankContainer>;
};

const RankContainer = styled.div`
  width: 270px;
  height: 170px;
  background: white;
  margin-top: 15px;
`;

export default Rank;
