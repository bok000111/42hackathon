import styled from "@emotion/styled";

const MentoCard = ({}: {
  intra: string;
  level: number;
  rating: number;
  subjects: string[];
  image: string | undefined;
  coalition: string;
}) => {
  return <MentoCardContainer></MentoCardContainer>;
};

const MentoCardContainer = styled.div``;

export default MentoCard;
